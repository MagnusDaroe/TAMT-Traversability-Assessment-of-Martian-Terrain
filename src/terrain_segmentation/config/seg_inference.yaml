# seg_inference.yaml - Configuration for YOLO inference

# Model configuration
model:
  model_dir: '/home/spacerob/tamt/src/terrain_segmentation/models/TAMT.pt'
  device: '0'  # Device to use: 0, 1, 2, etc., 'cpu'

# Dataset configuration
dataset:
  path: '/home/spacerob/tamt/dataset/AI4MARS_NAV_GOOD/images'  # Path to test images

# Inference parameters
inference:
  conf: 0.25          # Confidence threshold
  iou: 0.50           # IoU threshold for NMS
  imgsz: 640          # Input image size
  max_det: 300        # Maximum number of detections per image
  save_dir: '~/inference_results'  # Directory to save results

# Output settings
output:
  verbose: true       # Print detailed results
  save_txt: false     # Save results to txt files
  save_conf: false    # Save confidences in labels