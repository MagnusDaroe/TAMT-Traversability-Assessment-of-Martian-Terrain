The name of transformations in the code should be read as follows:

Example: "cam_to_global_transform_" should be read like the transform from camera to the global frame, meaning the camera frame seen from the global frame

