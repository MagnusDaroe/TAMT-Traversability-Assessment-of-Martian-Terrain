# Whenever someone wants to run the code in this branch they should use the following command to install all dependencies

rosdep install --from-paths src --ignore-src -r -y
colcon build

The name of transformations in the code should be read as follows:

Example: "cam_to_rover_transform_" should be read like the transform from camera to the rover frame, meaning the rover frame seen from camera frame 



